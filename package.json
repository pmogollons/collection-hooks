{
  "name": "collection-hooks",
  "version": "1.0.3",
  "description": "Extends Mongo.Collection with onInsert, onUpdate and onRemove hooks.",
  "scripts": {
    "test": "meteor test-packages --port 3534 ./ --driver-package test-in-browser",
    "publish": "meteor publish --release=3.0.2"
  },
  "author": "pmogollons",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.9.1",
    "eslint": "^9.9.1",
    "eslint-plugin-unused-imports": "^4.1.3",
    "globals": "^15.9.0",
    "typescript-eslint": "^8.2.0"
  }
}
